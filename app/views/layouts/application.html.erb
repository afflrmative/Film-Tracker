<!DOCTYPE html>
<html>
  <head>
    <title>FilmTrackers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light">
    <header class="bd-header bg-dark py-3 d-flex align-items-stretch border-bottom border-dark">
      </div>
        <div class="container-fluid d-flex align-items-center">
          <h1 class="d-flex align-items-center fs-4 text-white mb-0">
            Films-Tracker
          </h1>
          <span class="ms-auto btn btn-light">
            <%= link_to "Додати фільм", new_film_path %>
          </span>
          <span class="ms-auto link-light">
            <% if user_signed_in? %>
              <span>Вітаю, <%= current_user.email %></span>
              <span class="btn btn btn-light"><%= link_to 'Вийти', destroy_user_session_path, :method => :delete %></span>
            <% else %>
            <span class="p-2 btn btn-light"><%= link_to 'Увійти', new_user_session_path %></span> або <span class="p-2 btn btn-light"><%= link_to 'Зареєструватись', new_user_registration_path %></span>
            <% end %>
          </span>


        </div>
  </header>

    <div class="list-group">
      <%= yield %>
    </div>
  </body>
</html>
